package com.swe.controller.Meeting;

/**
 * Enum representing the mode of a session.
 */
public enum SessionMode {
    /** Test session mode. */
    TEST,

    /** Class session mode. */
    CLASS,
}
